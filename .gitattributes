# Normalise line endings so the repository is identical on every platform.
* text=auto eol=lf
*.png binary
*.pdf binary

# The site is generated; collapse it in diffs and reviews.
docs/** linguist-generated=true
